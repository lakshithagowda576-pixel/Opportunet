{
  "name": "root-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter \"@workspace/api-server\" --filter \"@workspace/job-portal\" --parallel dev",
    "install:all": "pnpm install",
    "seed": "pnpm -C Build-Project/Build-Project/scripts run seed --env-file=../.env"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
